# `branches/`

The branch model: series, flavors, and the invariants that hold them.

* [`model/`](model/) — series and their refs
* [`flavors/`](flavors/) — one source, many package names
* [`invariants/`](invariants/) — what every series guarantees
