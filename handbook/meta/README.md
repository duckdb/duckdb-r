# `meta/`

The documentation system itself:
how this tree is built and grows, how the prose this repository writes is written, where its evidence, its intent, and its
vocabulary are kept,
and what this repository decides for itself where the shared rules leave a choice open.

**A rule stated here binds every page above it.**
Shape and prose are conventions rather than preferences: a page that departs from them is corrected in review.
A preference nobody has enforced is not yet a rule, and does not belong on these pages.

**The shared pages have one home, and the local page carries the difference.**
The rules, the forms, the growth moves, authoring, style, and the evidence convention
live in [`cynkra/handbook-tools`](https://github.com/cynkra/handbook-tools), and the pages here at their paths point there,
so that every handbook built on them reads one text and a rule improved once is improved everywhere.
Everything this repository chooses differently, or in addition, is stated on the local page and nowhere else,
and `.handbook-source` at the root names the state of the source this handbook was last checked against.

* [`authoring/`](authoring/): what earns a sentence, and how long an entry runs
* [`experiments/`](experiments/): recorded evidence, and what earns a directory
* [`forms/`](forms/): the shapes a leaf, a link, a deepen line, a principle, and a backreference take
* [`glossary/`](glossary/): the terms of art, each linking its owner
* [`growth/`](growth/): the five moves that deepen the tree, and the protocol they share
* [`handbook/`](handbook/): the rules of this tree
* [`local/`](local/): what this repository decides for itself
* [`plans/`](plans/): active designs, superseded records
* [`style/`](style/): how a sentence is written: the line, the list, the number, the link
