% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage-config.R
\name{duckdb_storage_status}
\alias{duckdb_storage_status}
\title{Report where DuckDB stores extensions and secrets}
\usage{
duckdb_storage_status()
}
\value{
A data frame (class \code{"duckdb_storage_status"}) with one row per kind
of state and columns \code{kind}, \code{source}, and \code{directory}; its print method
renders a readable summary when the result is auto-printed.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{duckdb_storage_status()} reports the directory the duckdb R package would
currently use for downloaded extensions and for persisted secrets, and which
tier of the resolution policy chose it. It has no side effects: it never
prompts and never creates a directory, so an as-yet-uncreated \verb{~/.duckdb} is
reported as the per-session temporary default.

The location is chosen -- and can be changed -- as documented in
\link{duckdb_storage}: the \code{home} argument of \code{\link[=duckdb]{duckdb()}}, the \code{duckdb.home} option,
the \code{DUCKDB_R_HOME} environment variable, or an existing \verb{~/.duckdb}.
}
\examples{
duckdb_storage_status()
}
\seealso{
\link{duckdb_storage} for the storage policy this reports on, and
\code{\link[=duckdb]{duckdb()}} for the \code{home} argument.
}
