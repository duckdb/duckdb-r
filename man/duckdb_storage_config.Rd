% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage-config.R
\name{duckdb_storage_config}
\alias{duckdb_storage_config}
\alias{duckdb_extension_storage}
\alias{duckdb_secret_storage}
\alias{duckdb_storage_status}
\title{Configure where DuckDB stores extensions and secrets}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Choose where the duckdb R package keeps downloaded extensions and persisted
secrets, by writing a small \emph{marker file} that records the choice:
\itemize{
\item \code{duckdb_extension_storage()} -- set or move the extension cache.
\item \code{duckdb_secret_storage()} -- set or move the secret store.
\item \code{duckdb_storage_status()} -- report where each currently resolves.
}

By default both kinds live in a per-session temporary directory (extensions
use the package library when it is writable); these functions move them to a
location that survives across sessions. The same locations can also be set
without a marker by overriding with options and environment variables; the
full policy is documented in \link{duckdb_storage}.
}
\section{Functions}{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{duckdb_extension_storage(location, migrate = TRUE, conflict = "error")
duckdb_secret_storage(location, migrate = TRUE, conflict = "error")
duckdb_storage_status()
}\if{html}{\out{</div>}}

\code{duckdb_extension_storage()} and \code{duckdb_secret_storage()} write (or remove)
the marker for that one kind of state, so the two can be configured
independently. \code{duckdb_storage_status()} reports where each kind currently
resolves and which tier of the resolution policy chose it. The new location
takes effect for connections opened afterwards; existing connections are
unaffected.

There is no \code{ask} argument: calling a \verb{*_storage()} function is itself the
consent to write outside the temporary directory.
}

\section{Arguments}{
\describe{
\item{\code{location}}{The destination root, or an explicit path. Recognized
roots are \code{"session"} (the per-session temporary directory; also the
opt-out -- it removes the marker), \code{"user"} (\code{\link[tools:R_user_dir]{tools::R_user_dir()}}),
\code{"shared"} (\verb{~/.duckdb}, shared with the DuckDB CLI and Python client),
and -- for \code{duckdb_extension_storage()} only -- \code{"library"} (alongside
the installed package). See \link{duckdb_storage} for what each root means.}
\item{\code{migrate}}{If \code{TRUE} (the default), move the already-cached files
from the current location into the new one.}
\item{\code{conflict}}{How to resolve a name collision during migration:
\code{"error"} (the default) aborts and lists the collisions without moving
anything; \code{"ours"} lets the files being relocated overwrite the
destination; \code{"theirs"} keeps the destination files and drops the
colliding sources.}
}
}

\section{Value}{
The \verb{*_storage()} functions are called for their side effect (writing or
removing a marker, and optionally migrating files) and return the resolved
directory invisibly. \code{duckdb_storage_status()} returns a data frame with one
row per kind of state.
}

\section{Relationship to existing functions}{
\code{duckdb_secret_storage()} supersedes \code{\link[=duckdb_consolidate_secrets]{duckdb_consolidate_secrets()}}, which is
hard-deprecated: secret migration is now a \code{migrate} step of
\code{duckdb_secret_storage()}.
}

\seealso{
\link{duckdb_storage} for the storage policy these functions implement.
}
\keyword{internal}
