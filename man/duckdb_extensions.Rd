% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concepts-extensions.R
\name{duckdb_extensions}
\alias{duckdb_extensions}
\title{DuckDB extensions: what ships, what you install, and what can go wrong}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

DuckDB's functionality is split into a small core and a set of extensions.
This package bundles \strong{two} of them and downloads nothing on its own.
Everything else -- \code{icu}, \code{httpfs}, \code{spatial}, \code{json}, \code{h3}, and the rest --
is installed once, explicitly, and then loads itself automatically forever after.

That combination (automatic \emph{loading}, no automatic \emph{installing})
is the source of most extension surprises,
including the common report that a "bundled" extension is missing.
}
\section{What is bundled}{
The package statically links exactly two extensions:
\itemize{
\item \code{parquet} -- Parquet reading and writing
\item \code{core_functions} -- the bulk of DuckDB's scalar and aggregate functions
}

Nothing else is compiled in.
In particular \code{icu} is \strong{not} bundled,
so time-zone-aware operations that need it require an explicit install.

The DuckDB documentation describes several extensions as bundled or
statically linked. That refers to the DuckDB CLI and to the released
\code{libduckdb} shared library, both of which link a larger set.
It does not describe this R package, which is compiled from the same
sources with a different extension list.

To see the truth for your installation:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{dbGetQuery(con, "
  SELECT extension_name, installed, loaded, install_mode
  FROM duckdb_extensions()
  WHERE installed OR loaded
")
}\if{html}{\out{</div>}}

\code{install_mode} is \code{STATICALLY_LINKED} for the bundled two
and \code{REPOSITORY} for anything you installed yourself.
}

\section{Autoload is on, autoinstall is off}{
Two engine settings govern the behavior, and this package sets them apart:

\describe{
\item{\code{autoload_known_extensions} (\code{true})}{A known extension that is
already installed is loaded automatically the first time a query needs
it. You do not have to say \code{LOAD}.}
\item{\code{autoinstall_known_extensions} (\code{false})}{Nothing is downloaded
behind your back. A query that needs an extension you have never
installed fails with a message naming it, rather than silently reaching
out to the network.}
}

So the workflow is: install once, then forget about it.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{dbExecute(con, "INSTALL icu")   # once per DuckDB version and platform
# from now on, any query that needs ICU loads it by itself
}\if{html}{\out{</div>}}

The DuckDB CLI ships with \code{autoinstall_known_extensions} enabled, which is
why a statement that works there can fail here. To opt into the same
behavior for a session:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{dbExecute(con, "SET autoinstall_known_extensions = true")
}\if{html}{\out{</div>}}

The default is deliberate: an R package may not reach the network as a side
effect of an ordinary operation, so the first download has to be something
you asked for.
}

\section{Installed once per version and platform}{
Installed extensions are cached on disk under the extension directory --
see \link{duckdb_storage} for where that is and how to move it --
keyed by the DuckDB version and the platform string.

Two consequences:
\itemize{
\item After the package updates its vendored DuckDB, previously installed
extensions no longer match and must be installed again.
\item \verb{FORCE INSTALL <name>} re-downloads an extension whose cached copy is
stale or truncated.
}

Extension downloads are the caller's responsibility, and can fail on
machines without network access. Tests that need an extension should skip
when the install fails; see \link{duckdb_storage} for the pattern.
}

\section{Platforms}{
\describe{
\item{Windows (x86-64)}{R builds identify as \code{windows_amd64_mingw}.
Extensions built for that platform have been published since DuckDB
1.4.1; before that, many out-of-tree extensions returned HTTP 403 or 404
for it. If you see such an error, check the DuckDB version first.}
\item{Windows (ARM64)}{No extension distribution exists for
\code{windows_arm64_mingw} yet, so extension installs fail there.}
\item{macOS, Linux (x86-64, ARM64)}{Fully supported, subject to the
C++ standard library note below.}
\item{webR / WebAssembly}{Extension loading is not supported yet.}
}
}

\section{Linux builds not compiled with libstdc++}{
DuckDB's prebuilt extensions are compiled against \verb{libstdc++}.
Loading one into a package that was itself compiled against a different C++
standard library is ABI-incompatible and crashes R rather than failing
cleanly.

The package therefore disables extensions automatically on a Linux build
that was not compiled against \verb{libstdc++}, and says so once per session.
\code{INSTALL} and \code{LOAD} then error, and automatic loading is off.

Every other platform is unaffected -- macOS uses \verb{libc++}, but its prebuilt
extensions match.

Override the decision, in precedence order, with the \code{allow_extensions}
argument of \code{\link[=duckdb]{duckdb()}}, the \code{duckdb.allow_extensions} option, or the
\code{DUCKDB_R_ALLOW_EXTENSIONS} environment variable:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{con <- dbConnect(duckdb(allow_extensions = FALSE))  # accept, and stay quiet
con <- dbConnect(duckdb(allow_extensions = TRUE))   # try anyway; may crash R
}\if{html}{\out{</div>}}
}

\section{Troubleshooting}{
\describe{
\item{"Extension X is not installed"}{Expected: autoinstall is off.
Run \verb{INSTALL X} once.}
\item{An \code{icu} function is missing, or time zones behave oddly}{\code{icu} is
not bundled. \verb{INSTALL icu}, then reconnect or re-run the query.}
\item{HTTP 403 or 404 downloading an extension}{The extension has no build
for your platform and DuckDB version. Check \code{INSTALL} against a newer
DuckDB, and read the troubleshooting URL in the error, which names the
version, platform, and extension it looked for.}
\item{\code{INSTALL} errors saying extensions are disabled}{A Linux build not
compiled against \verb{libstdc++}; see the section above.}
\item{An extension worked yesterday and is gone today}{The package was
updated and vendors a new DuckDB. Install it again.}
\item{It works in the DuckDB CLI but not from R}{Most often autoinstall,
sometimes a different extension directory. Compare
\verb{SELECT * FROM duckdb_extensions()} on both sides.}
}
}

\seealso{
\code{\link[=duckdb]{duckdb()}} for the \code{allow_extensions} argument,
\link{duckdb_storage} for where extensions are cached.
}
